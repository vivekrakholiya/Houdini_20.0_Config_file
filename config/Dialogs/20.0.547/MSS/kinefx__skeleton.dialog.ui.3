// Magic: a0acfffe
// Automatically generated PSI dialog (PSI version 20.0.547)
// Thu Oct 30 19:54:20 2025


f_tmp = FLOAT_FIELD;	// dialog.top.ui

point_name.gad = ROW
{
    HSTRETCH JUSTIFY(left, center)

    {  }

    point_name.label = PARM_LABEL("Name", "point_name") ;
    PARM_FIELD(string, "point_name") VALUE(point_name.val) 
	LABELREF(point_name.label) HSTRETCH;

    {  }
}

child_compensate.gad = ROW
{
    
    {  }

    {  }
    { WIDTH(0.08) }

    PARM_BUTTON("child_compensate", "Child Compensate") toggle VALUE(child_compensate.val);

    {  }

    {  }
}

mode.gad = 
{
    LAYOUT(horizontal) JUSTIFY(left, center) 
    {  }

    mode.label = PARM_LABEL_SIMPLE("Mode", "mode") ;
    { WIDTH(0.09) }

    mode.menu;

    {  }

    {  }
}

scaleinheritance.gad = 
{
    LAYOUT(horizontal) JUSTIFY(left, center) 
    {  }

    scaleinheritance.label = PARM_LABEL_SIMPLE("Scale Inheritance", "scaleinheritance") ;
    { WIDTH(0.09) }

    scaleinheritance.menu;

    {  }

    {  }
}

jointplacement.gad = 
{
    LAYOUT(horizontal) JUSTIFY(left, center) 
    {  }

    jointplacement.label = PARM_LABEL_SIMPLE("Joint Placement", "jointplacement") ;
    { WIDTH(0.09) }

    jointplacement.menu;

    {  }

    {  }
}

enabletweak.gad = ROW
{
    
    {  }

    {  }
    { WIDTH(0.08) }

    PARM_BUTTON("enabletweak", "Tweak Mode") toggle VALUE(enabletweak.val);

    {  }

    {  }
}

# warning cpp warning: "GenV3/subdialog.top.ui":11 Macro "PARMSPACING" redefined.  Using later definition
# warning cpp warning: "GenV3/subdialog.top.ui":12 Macro "LAYOUTDIR" redefined.  Using later definition
# warning cpp warning: "GenV3/subdialog.top.ui":13 Macro "DO_JUSTIFY" redefined.  Using later definition
dialog_norm.gad = 
{
    LAYOUT(horizontal) SPACING(0.05) HSTRETCH JUSTIFY(left, center) UNSCALABLE
    point_name.gad; 
    child_compensate.gad; 
    mode.gad; 
    scaleinheritance.gad; 
    jointplacement.gad; 
    enabletweak.gad; 
} 
// dialog.bot.ui

dialog = OPENNOTIFIER
{
    LAYOUT(vertical) VALUE(open.val_) HSTRETCH

    dialog_norm.gad;
}
