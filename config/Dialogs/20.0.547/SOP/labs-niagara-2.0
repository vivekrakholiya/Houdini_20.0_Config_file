// Magic: d9d9b40d
// Automatically generated PSI dialog (PSI version 20.0.547)
// Tue Nov 18 10:38:37 2025


f_tmp = FLOAT_FIELD;	// dialog.top.ui

execute.gad = ROW
{
    JUSTIFY(left, center)
    { WIDTH(0.18) }

    { WIDTH(1.5) }
    { WIDTH(0.1) }

    PARM_BUTTON("execute", "Render")
	push VALUE(execute.val);

    { WIDTH(0.1) }
}

range_mode.gad = 
{
    LAYOUT(horizontal) JUSTIFY(left, center) HSTRETCH
    { WIDTH(0.18) }

    range_mode.label = PARM_LABEL_SIMPLE("Frame Range / Time Mode", "range_mode") WIDTH(1.5);
    { WIDTH(0.09) }

    range_mode.menu;

    { HSTRETCH }

    { WIDTH(0.1) }
}

f.gad = ROW
{
   HSTRETCH
   { WIDTH(0.18) }

   f.label = PARM_LABEL("Start/End/Inc", "f") WIDTH(1.5);
   PARM(float, 3, "f", none, none, "#1,#2,#3")
	RANGE(0,10)  LABELREF(f.label)
	HSTRETCH;

    { WIDTH(0.1) }
}

take.gad = 
{
    LAYOUT(horizontal) JUSTIFY(left, center) HSTRETCH
    { WIDTH(0.18) }

    take.label = PARM_LABEL_SIMPLE("Render with Take", "take") WIDTH(1.5);
    { WIDTH(0.09) }

    take.menu;

    { HSTRETCH }

    { WIDTH(0.1) }
}

mkpath.gad = ROW
{
    HSTRETCH
    { WIDTH(0.18) }

    { WIDTH(1.5) }
    { WIDTH(0.08) }

    PARM_BUTTON("mkpath", "Create Intermediate Directories") toggle VALUE(mkpath.val);

    { HSTRETCH }

    { WIDTH(0.1) }
}

outputpath.gad = 
{
 LAYOUT(horizontal) HSTRETCH JUSTIFY(left, center)
 { WIDTH(0.18) }

 outputpath.label = PARM_LABEL("Output Path", "outputpath") WIDTH(1.5);

 PARM_FIELD (pathstring, "outputpath") LABELREF(outputpath.label)
	VALUE(outputpath.val) HSTRETCH;

    outputpath.menu SIZE(0.26, 0.26) LOOK(menuiconbutton);

 ACTION_ICONBUTTON "BUTTONS_chooser_file" VALUE(outputpath.plus) LOOKFLAT
    HELP("Open floating file chooser") UI_FLAT_ICON_BTN_SIZE_SMALL;

 { WIDTH(0.1) }
}

ptrenames.parm =
{
    LAYOUT(horizontal) HSTRETCH JUSTIFY(left, center)

    PARM_LABEL_SIMPLE("Number of Renames", "ptrenames") WIDTH(1.5);
    { WIDTH(0.1) }

    ROW
    {
	HSTRETCH(0.5) JUSTIFY(left, center) VALUE(ptrenames.val)

        ptrenames.field = PARM_FIELD (integer, "ptrenames")
				VALUE(ptrenames.val) HSTRETCH(0.5)
				 PREF_WIDTH(0.5);

        ROW
        {
	     LOOK(lightline) VMARGIN(1p)

   	     INCREMENT_ICONBUTTON VALUE(ptrenames.more.val)	
	    	HELP("Add Another Instance") SIZE(0.2, 0.24);
	     DECREMENT_ICONBUTTON VALUE(ptrenames.less.val)	
	        HELP("Remove Last Instance") SIZE(0.2, 0.24);

	}

	ACTION_BUTTON "Clear" VALUE(ptrenames.clear.val);
    }

    { HSTRETCH }
}

ptrenames.multilist = 
{
    LAYOUT(vertical) HSTRETCH MARGIN(0.05)
}

ptrenames.gad =
{
    LAYOUT(vertical) HSTRETCH
    HMARGIN(0.18, 0.1) NO_MARGIN_SPACING_SCALE
    SWITCHER tight
    {
	LAYOUT(overlap!)
        VALUE(ptrenames.multilist_hide_buttons.val)
	{
	    LAYOUT(vertical) HSTRETCH
	    ptrenames.parm;
	    {LOOK(none) HEIGHT(0.02)}
	}
	{
	}
    }
    ptrenames.multilist;
}

overwrite.gad = ROW
{
    HSTRETCH
    { WIDTH(0.18) }

    { WIDTH(1.5) }
    { WIDTH(0.08) }

    PARM_BUTTON("overwrite", "Overwrite Existing Attributes") toggle VALUE(overwrite.val);

    { HSTRETCH }

    { WIDTH(0.1) }
}

ptkeep.gad = ROW
{
    HSTRETCH JUSTIFY(left, center)

    { WIDTH(0.18) }

    ptkeep.label = PARM_LABEL("Keep Attributes", "ptkeep") WIDTH(1.5);
    PARM_FIELD(string, "ptkeep") VALUE(ptkeep.val) 
	LABELREF(ptkeep.label) HSTRETCH;

    ptkeep.menu SIZE(0.26, 0.26) LOOK(menuiconbutton);

    { WIDTH(0.1) }
}

attr_types.parm =
{
    LAYOUT(horizontal) HSTRETCH JUSTIFY(left, center)

    PARM_LABEL_SIMPLE("Cast Attributes", "attr_types") WIDTH(1.5);
    { WIDTH(0.1) }

    ROW
    {
	HSTRETCH(0.5) JUSTIFY(left, center) VALUE(attr_types.val)

        attr_types.field = PARM_FIELD (integer, "attr_types")
				VALUE(attr_types.val) HSTRETCH(0.5)
				 PREF_WIDTH(0.5);

        ROW
        {
	     LOOK(lightline) VMARGIN(1p)

   	     INCREMENT_ICONBUTTON VALUE(attr_types.more.val)	
	    	HELP("Add Another Instance") SIZE(0.2, 0.24);
	     DECREMENT_ICONBUTTON VALUE(attr_types.less.val)	
	        HELP("Remove Last Instance") SIZE(0.2, 0.24);

	}

	ACTION_BUTTON "Clear" VALUE(attr_types.clear.val);
    }

    { HSTRETCH }
}

attr_types.multilist = 
{
    LAYOUT(vertical) HSTRETCH MARGIN(0.05)
}

attr_types.gad =
{
    LAYOUT(vertical) HSTRETCH
    HMARGIN(0.18, 0.1) NO_MARGIN_SPACING_SCALE
    SWITCHER tight
    {
	LAYOUT(overlap!)
        VALUE(attr_types.multilist_hide_buttons.val)
	{
	    LAYOUT(vertical) HSTRETCH
	    attr_types.parm;
	    {LOOK(none) HEIGHT(0.02)}
	}
	{
	}
    }
    attr_types.multilist;
}

attr_clamp.gad = ROW
{
    HSTRETCH
    { WIDTH(0.18) }

    { WIDTH(1.5) }
    { WIDTH(0.08) }

    PARM_BUTTON("attr_clamp", "Clamp Attributes That Are Outside Type Range") toggle VALUE(attr_clamp.val);

    { HSTRETCH }

    { WIDTH(0.1) }
}

export_current_frame_as_time_zero.gad = ROW
{
    HSTRETCH
    { WIDTH(0.18) }

    { WIDTH(1.5) }
    { WIDTH(0.08) }

    PARM_BUTTON("export_current_frame_as_time_zero", "Export Current Frame As time == 0") toggle VALUE(export_current_frame_as_time_zero.val);

    { HSTRETCH }

    { WIDTH(0.1) }
}

export_start_frame_as_time_zero.gad = ROW
{
    HSTRETCH
    { WIDTH(0.18) }

    { WIDTH(1.5) }
    { WIDTH(0.08) }

    PARM_BUTTON("export_start_frame_as_time_zero", "Export Start Frame As time == 0") toggle VALUE(export_start_frame_as_time_zero.val);

    { HSTRETCH }

    { WIDTH(0.1) }
}

fd_timesettings.gad = 

GROUP simple "Time Settings" flat 
{ 
    VALUE(fd_timesettings.gad.val) LAYOUT(vertical) HSTRETCH 
	    export_current_frame_as_time_zero.gad;		
	    export_start_frame_as_time_zero.gad;		
} 

fd_attributes.gad = 

GROUP simple "Attributes" flat 
{ 
    VALUE(fd_attributes.gad.val) LAYOUT(vertical) HSTRETCH 
	    ptrenames.gad;		
	    overwrite.gad;		
	    ptkeep.gad;		
	    attr_types.gad;		
	    attr_clamp.gad;		
} 

fd_rendersettings.gad = 

GROUP simple "ROP Settings" flat 
{ 
    VALUE(fd_rendersettings.gad.val) LAYOUT(vertical) HSTRETCH 
	    range_mode.gad;		
	    f.gad;		
	    take.gad;		
	    mkpath.gad;		
	    outputpath.gad;		
} 

fd_general.gad = 
{
    LAYOUT(vertical) HSTRETCH

    TABDIALOG2 flat "fd_general.gad"
    { 
	VALUE(fd_general.gad.val) STRETCH(1,0)

	TAB "General"
	{
	    LAYOUT(vertical) STRETCH

            HMARGIN(0) VMARGIN(2p) SPACING(0, 2p)
	    fd_rendersettings.gad;		
	    fd_attributes.gad;		

	    { STRETCH(1,1) }
	}

	TAB "Advanced"
	{
	    LAYOUT(vertical) STRETCH

            HMARGIN(0) VMARGIN(2p) SPACING(0, 2p)
	    fd_timesettings.gad;		

	    { STRETCH(1,1) }
	}
    } 
}

dialog_norm.gad = 
{
    LAYOUT(vertical) SPACING(0.02) HSTRETCH 
    execute.gad; 
    fd_general.gad; 
} 
// dialog.bot.ui

extrastuffstowbar_minimize.val := 1;

dialog = OPENNOTIFIER
{
    LAYOUT(vertical) SPACING(0.02) LOOK(bg) VALUE(open.val_) HSTRETCH

    { HEIGHT(1p) LOOK(bg) HSTRETCH }

    dialog_norm.gad;
}
