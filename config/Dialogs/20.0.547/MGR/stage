// Magic: 1b5fbec3
// Automatically generated PSI dialog (PSI version 20.0.547)
// Tue Jun  3 10:57:41 2025


f_tmp = FLOAT_FIELD;	// dialog.top.ui

pinnedprims.gad = ROW
{
    HSTRETCH JUSTIFY(left, center)

    { WIDTH(0.18) }

    pinnedprims.label = PARM_LABEL("Favorite Primitives", "pinnedprims") WIDTH(1.5);
    PARM_FIELD(string, "pinnedprims") VALUE(pinnedprims.val) 
	LABELREF(pinnedprims.label) HSTRETCH;

    { WIDTH(0.1) }
}

resolvercontextassetpath.gad = 
{
 LAYOUT(horizontal) HSTRETCH JUSTIFY(left, center)
 { WIDTH(0.18) }

 resolvercontextassetpath.label = PARM_LABEL("Resolver Context Asset Path", "resolvercontextassetpath") WIDTH(1.5);

 PARM_FIELD (pathstring, "resolvercontextassetpath") LABELREF(resolvercontextassetpath.label)
	VALUE(resolvercontextassetpath.val) HSTRETCH;

    resolvercontextassetpath.menu SIZE(0.26, 0.26) LOOK(menuiconbutton);

 ACTION_ICONBUTTON "BUTTONS_chooser_file" VALUE(resolvercontextassetpath.plus) LOOKFLAT
    HELP("Open floating file chooser") UI_FLAT_ICON_BTN_SIZE_SMALL;

 { WIDTH(0.1) }
}

resolvercontextstringcount.parm =
{
    LAYOUT(horizontal) HSTRETCH JUSTIFY(left, center)

    PARM_LABEL_SIMPLE("Number of Resolver Context Strings", "resolvercontextstringcount") WIDTH(1.5);
    { WIDTH(0.1) }

    ROW
    {
	HSTRETCH(0.5) JUSTIFY(left, center) VALUE(resolvercontextstringcount.val)

        resolvercontextstringcount.field = PARM_FIELD (integer, "resolvercontextstringcount")
				VALUE(resolvercontextstringcount.val) HSTRETCH(0.5)
				 PREF_WIDTH(0.5);

        ROW
        {
	     LOOK(lightline) VMARGIN(1p)

   	     INCREMENT_ICONBUTTON VALUE(resolvercontextstringcount.more.val)	
	    	HELP("Add Another Instance") SIZE(0.2, 0.24);
	     DECREMENT_ICONBUTTON VALUE(resolvercontextstringcount.less.val)	
	        HELP("Remove Last Instance") SIZE(0.2, 0.24);

	}

	ACTION_BUTTON "Clear" VALUE(resolvercontextstringcount.clear.val);
    }

    { HSTRETCH }
}

resolvercontextstringcount.multilist = 
{
    LAYOUT(vertical) HSTRETCH MARGIN(0.05)
}

resolvercontextstringcount.gad =
{
    LAYOUT(vertical) HSTRETCH
    HMARGIN(0.18, 0.1) NO_MARGIN_SPACING_SCALE
    SWITCHER tight
    {
	LAYOUT(overlap!)
        VALUE(resolvercontextstringcount.multilist_hide_buttons.val)
	{
	    LAYOUT(vertical) HSTRETCH
	    resolvercontextstringcount.parm;
	    {LOOK(none) HEIGHT(0.02)}
	}
	{
	}
    }
    resolvercontextstringcount.multilist;
}

variantselectioncount.parm =
{
    LAYOUT(horizontal) HSTRETCH JUSTIFY(left, center)

    PARM_LABEL_SIMPLE("Number of Variant Selection Fallback Rules", "variantselectioncount") WIDTH(1.5);
    { WIDTH(0.1) }

    ROW
    {
	HSTRETCH(0.5) JUSTIFY(left, center) VALUE(variantselectioncount.val)

        variantselectioncount.field = PARM_FIELD (integer, "variantselectioncount")
				VALUE(variantselectioncount.val) HSTRETCH(0.5)
				 PREF_WIDTH(0.5);

        ROW
        {
	     LOOK(lightline) VMARGIN(1p)

   	     INCREMENT_ICONBUTTON VALUE(variantselectioncount.more.val)	
	    	HELP("Add Another Instance") SIZE(0.2, 0.24);
	     DECREMENT_ICONBUTTON VALUE(variantselectioncount.less.val)	
	        HELP("Remove Last Instance") SIZE(0.2, 0.24);

	}

	ACTION_BUTTON "Clear" VALUE(variantselectioncount.clear.val);
    }

    { HSTRETCH }
}

variantselectioncount.multilist = 
{
    LAYOUT(vertical) HSTRETCH MARGIN(0.05)
}

variantselectioncount.gad =
{
    LAYOUT(vertical) HSTRETCH
    HMARGIN(0.18, 0.1) NO_MARGIN_SPACING_SCALE
    SWITCHER tight
    {
	LAYOUT(overlap!)
        VALUE(variantselectioncount.multilist_hide_buttons.val)
	{
	    LAYOUT(vertical) HSTRETCH
	    variantselectioncount.parm;
	    {LOOK(none) HEIGHT(0.02)}
	}
	{
	}
    }
    variantselectioncount.multilist;
}

insertionpointdescriptor.gad = ROW
{
    HSTRETCH JUSTIFY(left, center)

    { WIDTH(0.18) }

    insertionpointdescriptor.label = PARM_LABEL("Insertion Point Descriptor", "insertionpointdescriptor") WIDTH(1.5);
    PARM_FIELD(string, "insertionpointdescriptor") VALUE(insertionpointdescriptor.val) 
	LABELREF(insertionpointdescriptor.label) HSTRETCH;

    { WIDTH(0.1) }
}

rendergallerysource.gad = 
{
 LAYOUT(horizontal) HSTRETCH JUSTIFY(left, center)
 { WIDTH(0.18) }

 rendergallerysource.label = PARM_LABEL("Render Gallery Source", "rendergallerysource") WIDTH(1.5);

 PARM_FIELD (pathstring, "rendergallerysource") LABELREF(rendergallerysource.label)
	VALUE(rendergallerysource.val) HSTRETCH;

    rendergallerysource.menu SIZE(0.26, 0.26) LOOK(menuiconbutton);

 ACTION_ICONBUTTON "BUTTONS_chooser_file" VALUE(rendergallerysource.plus) LOOKFLAT
    HELP("Open floating file chooser") UI_FLAT_ICON_BTN_SIZE_SMALL;

 { WIDTH(0.1) }
}

dialog_norm.gad = 
{
    LAYOUT(vertical) SPACING(0.02) HSTRETCH 
    pinnedprims.gad; 
    resolvercontextassetpath.gad; 
    resolvercontextstringcount.gad; 
    variantselectioncount.gad; 
    insertionpointdescriptor.gad; 
    rendergallerysource.gad; 
} 
// dialog.bot.ui

extrastuffstowbar_minimize.val := 1;

dialog = OPENNOTIFIER
{
    LAYOUT(vertical) SPACING(0.02) LOOK(bg) VALUE(open.val_) HSTRETCH

    { HEIGHT(1p) LOOK(bg) HSTRETCH }

    dialog_norm.gad;
}
