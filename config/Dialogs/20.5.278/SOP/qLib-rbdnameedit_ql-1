// Magic: 27c2bbdd
// Automatically generated PSI dialog (PSI version 20.5.278)
// Mon Aug  5 21:59:03 2024


f_tmp = FLOAT_FIELD;	// dialog.top.ui

nameattrib.gad = ROW
{
    HSTRETCH JUSTIFY(left, center)

    { WIDTH(0.18) }

    nameattrib.label = PARM_LABEL("Name Attribute(s)", "nameattrib") WIDTH(1.5);
    PARM_FIELD(string, "nameattrib") VALUE(nameattrib.val) 
	LABELREF(nameattrib.label) HSTRETCH;

    nameattrib.menu SIZE(0.26, 0.26) LOOK(menuiconbutton);

    { WIDTH(0.1) }
}

filters.parm =
{
    LAYOUT(horizontal) HSTRETCH JUSTIFY(left, center)

    PARM_LABEL_SIMPLE("Number of Filters", "filters") WIDTH(1.5);
    { WIDTH(0.1) }

    ROW
    {
	HSTRETCH(0.5) JUSTIFY(left, center) VALUE(filters.val)

        filters.field = PARM_FIELD (integer, "filters")
				VALUE(filters.val) HSTRETCH(0.5)
				 PREF_WIDTH(0.5);

        ROW
        {
	     LOOK(lightline) VMARGIN(1p)

   	     INCREMENT_ICONBUTTON VALUE(filters.more.val)	
	    	HELP("Add Another Instance") SIZE(0.2, 0.24);
	     DECREMENT_ICONBUTTON VALUE(filters.less.val)	
	        HELP("Remove Last Instance") SIZE(0.2, 0.24);

	}

	ACTION_BUTTON "Clear" VALUE(filters.clear.val);
    }

    { HSTRETCH }
}

filters.multilist = 
{
    LAYOUT(vertical) HSTRETCH MARGIN(0.05)
}

filters.gad =
{
    LAYOUT(vertical) HSTRETCH
    HMARGIN(0.18, 0.1) NO_MARGIN_SPACING_SCALE
    SWITCHER tight
    {
	LAYOUT(overlap!)
        VALUE(filters.multilist_hide_buttons.val)
	{
	    LAYOUT(vertical) HSTRETCH
	    filters.parm;
	    {LOOK(none) HEIGHT(0.02)}
	}
	{
	}
    }
    filters.multilist;
}

folder0.gad = 

GROUP simple "Name Edit Filters" flat 
{ 
    VALUE(folder0.gad.val) LAYOUT(vertical) HSTRETCH 
	    filters.gad;		
} 

dialog_norm.gad = 
{
    LAYOUT(vertical) SPACING(0.02) HSTRETCH 
    nameattrib.gad; 
    folder0.gad; 
} 
// dialog.bot.ui

extrastuffstowbar_minimize.val := 1;

dialog = OPENNOTIFIER
{
    LAYOUT(vertical) SPACING(0.02) LOOK(bg) VALUE(open.val_) HSTRETCH

    { HEIGHT(1p) LOOK(bg) HSTRETCH }

    dialog_norm.gad;
}
