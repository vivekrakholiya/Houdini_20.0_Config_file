// Magic: af3567a9
// Automatically generated PSI dialog (PSI version 20.5.278)
// Sun Jan  5 15:46:04 2025


f_tmp = FLOAT_FIELD;	// dialog.top.ui

group.gad = ROW
{
    HSTRETCH JUSTIFY(left, center)

    { WIDTH(0.18) }

    group.label = PARM_LABEL("Group", "group") WIDTH(1.5);
    PARM_FIELD(string, "group") VALUE(group.val) 
	LABELREF(group.label) HSTRETCH;

    group.menu SIZE(0.26, 0.26) LOOK(menuiconbutton);

    ACTION_ICONBUTTON "BUTTONS_reselect" VALUE(group.script_action)
	HELP("Select geometry from an available viewport.")
	UI_FLAT_ICON_BTN_SIZE_SMALL LOOKFLAT;

    { WIDTH(0.1) }
}

grouptype.gad = 
{
    LAYOUT(horizontal) JUSTIFY(left, center) HSTRETCH
    { WIDTH(0.18) }

    grouptype.label = PARM_LABEL_SIMPLE("Group Type", "grouptype") WIDTH(1.5);
    { WIDTH(0.09) }

    grouptype.menu;

    { HSTRETCH }

    { WIDTH(0.1) }
}

radius.gad = 
{

 LAYOUT(horizontal) HSTRETCH
 { WIDTH(0.18) }

 radius.label = PARM_LABEL("Radius", "radius") WIDTH(1.5);
 PARM(float, 1, "radius", none, none) RANGE(0,10)
	 LABELREF(radius.label) HSTRETCH;

  { WIDTH(0.1) }
}

minpt.gad = 
{

 LAYOUT(horizontal) HSTRETCH
 { WIDTH(0.18) }

 minpt.label = PARM_LABEL("Minimum Points", "minpt") WIDTH(1.5);
 PARM(integer, 1, "minpt", none, none) RANGE(0,10)
	 LABELREF(minpt.label) HSTRETCH;

  { WIDTH(0.1) }
}

maxpt.gad = 
{

 LAYOUT(horizontal) HSTRETCH
 { WIDTH(0.18) }

 maxpt.label = PARM_LABEL("Maximum Points", "maxpt") WIDTH(1.5);
 PARM(integer, 1, "maxpt", none, none) RANGE(0,10)
	 LABELREF(maxpt.label) HSTRETCH;

  { WIDTH(0.1) }
}
usenameattrib.row.join =		
{

  LAYOUT(horizontal) HSTRETCH JUSTIFY(left, center)
  HMARGIN(0.18, 0.1) NO_MARGIN_SPACING_SCALE
usenameattrib.join =		
{

  LAYOUT(horizontal) HSTRETCH JUSTIFY(left, center)
  HMARGIN(0, 0) NO_MARGIN_SPACING_SCALE

usenameattrib.gad = PARM_BUTTON("usenameattrib", "") toggle VALUE(usenameattrib.val) RETAIN_SIZE_WHEN_HIDDEN;

nameattrib.gad = ROW
{
    HSTRETCH JUSTIFY(left, center)

    {  }

    nameattrib.label = PARM_LABEL("Name Attrib", "nameattrib") WIDTH(1.32);
    PARM_FIELD(string, "nameattrib") VALUE(nameattrib.val) 
	LABELREF(nameattrib.label) HSTRETCH;

    nameattrib.menu SIZE(0.26, 0.26) LOOK(menuiconbutton);

    {  }
}
}	
}	// Template join.bot


folder1.gad = 

GROUP simple "Partitioning" flat 
{ 
    VALUE(folder1.gad.val) LAYOUT(vertical) HSTRETCH 
	    usenameattrib.row.join;		
} 

folder0.gad = 

GROUP simple "Capture" flat 
{ 
    VALUE(folder0.gad.val) LAYOUT(vertical) HSTRETCH 
	    radius.gad;		
	    minpt.gad;		
	    maxpt.gad;		
	    folder1.gad;		
} 

dialog_norm.gad = 
{
    LAYOUT(vertical) SPACING(0.02) HSTRETCH 
    group.gad; 
    grouptype.gad; 
    folder0.gad; 
} 
// dialog.bot.ui

extrastuffstowbar_minimize.val := 1;

dialog = OPENNOTIFIER
{
    LAYOUT(vertical) SPACING(0.02) LOOK(bg) VALUE(open.val_) HSTRETCH

    { HEIGHT(1p) LOOK(bg) HSTRETCH }

    dialog_norm.gad;
}
